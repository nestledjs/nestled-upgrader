node_modules/

# Instance-specific operational files — not for version control.
# Copy upgrader.config.example.yaml to upgrader.config.yaml and customize.
upgrader.config.yaml
upgrader.state.yaml
upgrades/
patches/
reports/
.claude/**

# Timestamped console logs written by the Playwright MCP server during a session.
.playwright-mcp/
